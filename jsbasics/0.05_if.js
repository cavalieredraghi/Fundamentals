var isOn = true;
var isHot = false;

if (isOn) {
	console.log("Man, that game was so sick!");
}
if (isOn===true) {
	console.log("Dude, right!");
}

if (isOn && isHot) {
	console.log("Did you see that headshot? It was so glitched!")
}
if (isOn || isHot) {
	console.log ("Nah the other guy just lagged.")
}
if (!isHot) {
	console.log ("Yah he was just a bad player.")
}